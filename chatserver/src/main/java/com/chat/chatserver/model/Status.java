package com.chat.chatserver.model;

public enum Status {
    JOIN,
    MESSAGE,
    LEAVE
}

/*
JOIN: quando um cliente entra no chat;
MESSAGE: quando uma mensagem é enviada;
LEAVE: quando um cliente sai do chat; */